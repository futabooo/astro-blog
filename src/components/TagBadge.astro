---
export interface Props {
  name: string;
  count?: number;
}
const { name, count } = Astro.props;
---

<a
  class="badge badge-outline p-3"
  href={`/tags/${name.toLowerCase().replace(/ /g, "-")}`}
  >#{name} {count && `(${count})`}</a
>
